# Owns: concrete implementations of source, sink, and db ports.
# Must not: contain domain logic (that lives in core).
# May import: warehouse_app.core (types only), warehouse_app.config, third-party libs.
